// 1
//         there is 6 data type to be fear i know this types 
        
//         typeof 12; // = number
//         typeof "hello world"; // = string
//         typeof i; // = undefind
//         typeof true,false // Booleans
//         symbol
//         Null

// 2
//         by loop we can do something repeatedly for example if i want to add number from 1 to 100 with an array i can use loop to add number if i add an array line 
//         this is just a simple examlpe of num and line reapet

//         var step;
//         for (step = 0; step < 5; step++) {
//         // Runs 5 times, with values of step 0 through 4.
//         console.log('Walking east one step');
//         } 
//         for (let i = 0; i < 101; i++) {
//         // Runs 5 times, with values of step 0 through 4.
//         console.log(i);
//         }

// 3
//         from what i handrstand for javascript is file.js, html is file.html and for css is file.css we can easelly change a file by " . "and type of file

// 4
//           ?????????????????????     

// 5 

//         function myFunction(a, b){
//             a = 1;
//             b = 1;
//             return a + b * 1;
//         }
//         myFunction();

// 6
//         ?????????????????????????

// 7
//         ?????????????????????????

// 8
//         const me = {
//             Name: "Abdul Satar",
//             Last_Name: "Sherzai Yadegari",
//             FavoriteMovie: "Hip man"
//         };

// 9
//         ???????????????????????????

// 10

//         const id = document.getElementById("hack-youe-future");

// 11        

//         var a = 5;
//         var x = typeof a;
//         output = number because it's same as 
//         var a = 5;
//         let x = a;
//         typeof x;

// 12

//         var a = 5.5;
//         var x = typeof a;
//         again
//         output = number

// 13
//         outuput = undefind

// 14
//         output = string because we called ' d ' outsite of function and then we decleared d by x then console x and decleared the function

// 15
//         it will be same string beacuse we decleared function aftre 'd' 

// 16
//         the value of x is false beacuse x become a but a is not equall b it false

// 17
//         the value of x is undefind because we didn't declared it before function !

// 18
//         a is undefind and the typeof undefind is undefind. x = undefind

// 19
//         the output is { alice talks a lot, bob listens a lot, eve tries to listen and diffie stops eve } because foreach is repeating the array and object by a,b, and d. if statment = if new sentence added use { , } but for las use and

// 20
//         ?????????????????????

// 21
//         from what i handstand we just call functin inside onother function 
//         function foo(func) {
//                 // TODO     
//         bar()    
//         }
//             function bar() {
//                 console.log('Hello, I am bar!');
//             }
        
//             foo(bar);

// 22
//         output is going to be 
//         nothing 
//         special 
//         because first something is qual to nothing we delared by doit then something changed to special by doItAgain 

// 23
//         ???????????????????????

//                 /////////////////////////////////////
                
//                              Javascript 3         //
                
//                 /////////////////////////////////////

// 1
//         boolean

// 2
//         output is 10 because its function doesnt do any thing

// 3
        
//         let a = true;
//         let x;
//         if (a === true) {
//         x = 10  
//                 }else {
//                 x = 20
//                 }
        
// 4
//         but rounded ?
//         let b = 10.7;
//         let x = b;

// 5
//         i think question 4 & 5 is same
//         let s = 'HACK YOUR FUTURE';
//         let x = s;
//         console.log(x)

// 6
//         the best way to do is reduse 
//         let a = ["h","a","c","k"," ","y","o","u","r"," ","f","u","t","u","r","e"];
//         let x = (accumulator, currentValue) => accumulator + currentValue;

//         console.log(a.reduce(x));
//         expected output: hack yor future

// 7
//         let o = {
//                 start: 'hack your',
//                 end: ' future'
//         };
        
//         let x = o.start + o.end;
        
//         console.log(x)

// 8
//         NaN is Not a Number

// 9
//         undefind because a is just varuble without a value 

// 10

//         function myFunction(a) {
//                 a = 10;
//                 a = a * 2;
//                 return a
//         }
//         myFunction();

// 11
//         function myFunction(x) {
//           x = x * 2;
//           return x
//         }
//         myFunction(10);

// 12
//         output = true 
//         x = 0
//         because we decleared 0 by x that mean 0 is true 

// 13
//         ithink output is hack your future 

// 14
//         console.log after return! no we return and console log is same we can use only one however we can use 2 console.log 
//         output is blue and yrllow 

// 15
//         output is blue green yellow 
//         because we console.log green before a in the x function | because 'y' is called inside of 'x'

// 16
        //this.a = 'yay';
        // let myObject = {
        //         a: 'nay',
        //         printHisA: function() {
        //         console.log(this.a);
        //         }
        // };
        
        // myObject.printHisA();    output = ney

//17
        //this.a = 'yay';
        // let myObject = {
        //         a: 'nay',
        //         printHisA: function() {
        //         console.log(this.a);
        //         }.bind(this)
        // };
        
        // myObject.printHisA();    output = yey

// 18
//         output is 0 because loop start from 0 to 1 and 1 is 0

// 19
//         output is 14 and the typof 14 is number!
//         map multiply the numbers and reduse is + 

// 20
//         icould use this way with object and map!
//         let numbers = [1, 2, 3, 4];
//         numbers.map((n) =>{return n * 2;});

//         let numbers = [1, 2, 3, 4];
//         let collectionNumbers = {
//         theOrginialNumbers: numbers,
//         The_doubled_numbers_are: numbers.map(function(n) { return n*2; })
//         };
//         console.log(collectionNumbers);

// 21
        

// 22
//         the value of x is 10 because first it reduse and then collect all the numbers
        
// 23
//         I dont handrstand the question!

// 24
//         sorry but its the first time i heard 

// 25
//         ES6 is the upcoming sixth major release of the ECMAScript ECMAScript 6 the proper name for javascript that came from switzerland.

// 26
//         its not number so we can say nan becuase this.someProperty is not a number to multiply.
        
// 27
//         tis is a complex way of map reduse and filter that i think we should study more about.

// 28
//         you just console.log the catch before map and after so it will print oh no saved / Did we fail

// 29
//         the status is rejected 

// 30
        

// 31
//         it should be undefind because we never did a varuble like that {let a = 3,b = 5}

// 32
//         teachers = [
//                 { first:"Onne", last:"Gorte", github:"https://github.com/onnlucky", "nickname":"Lucky"},
//                 { first:"Unmesh", last:"Vrije", github:"https://github.com/unmeshvrije", "nickname":"git lord" },
//                 { first:"Thomas", last:"Meijers", github:"https://github.com/TMMeijers", "nickname":"this."},
//                 { first:"Laurens", last:"Aarnoudse", github:"https://github.com/Razpudding", "nickname":"mother loving"},
//         ];
        
//         teachers.filter(t => t.first == "Laurens" && t.last == "Aarnoudse")[0].github 
//         Also
//         console.log("First name: "+ teachers[3].first)
//         console.log("Last name: " + teachers[3].last)
//         console.log("Github URL: " + teachers[3].github)

// 33
//         name is Unmesh nickname is undefind because we can use just with [1].first or last not [1][3]
//         this code is the correct code for that to print name and nick name.
//         console.log("name: " + teachers[1].first + "; nickname: " + teachers[1].last);

// 34
//         no idea